# Déploiement staging contrôlé — post-#23 (tickets #17 à #23)

**Date** : 2026-09-17
**Fait suite à** : `docs/claude-reports/2026-09-16_diagnostic_staging_post-23.md` (écrit dans
`/tmp` lors du diagnostic, cause confirmée : aucun déploiement réel depuis le ticket #14).
**Autorisation** : déploiement staging explicitement demandé par ChatGPT après validation
du diagnostic.

---

## 1. Résumé

Déploiement staging réussi. `jury-central.service` exécute désormais le commit
`b76c36e` (`develop`, à jour jusqu'au ticket #23 inclus). `seed-db` exécuté avec succès :
colonne `space` ajoutée, 16 blocs reclassés COURSE/PRACTICE/EXAM en place, 2 blocs
obsolètes retirés, 6 nouveaux blocs créés (les 4 exercices structurés + 2 blocs Markdown
réduits du ticket #21) — **aucune perte de contenu**, **aucun `reset-db`**. Toutes les
routes Cours/S'entraîner/S'évaluer répondent 200 en local et en public, avec le contenu
attendu dans chaque espace, pour MC01, MC02, MC03 et Mathématiques. Second `seed-db` =
strictement 0 modification (idempotence confirmée au niveau du hash MD5 du fichier). Aucune
clé OpenAI configurée : un appel de génération IA renvoie bien 503, proprement. Aucune
modification de nginx, Certbot, systemd, ou tout autre service AWS.

---

## 2. Étapes 1 à 4 — synchronisation

```
$ git checkout develop
$ git pull --ff-only origin develop
Updating 1b29b43..b76c36e
Fast-forward
 [16 fichiers, 3085 insertions]
$ git status --short
(vide)
$ git rev-parse HEAD
b76c36e0f7570d9785d0945d25916e0d37a28410
$ git rev-parse origin/develop
b76c36e0f7570d9785d0945d25916e0d37a28410
```

`HEAD == origin/develop`, working tree propre, avant toute exécution de script.

---

## 3. Étape 5 — `./scripts/deploy_staging.sh`

Script exécuté tel quel (aucune modification). Sortie complète :

```
[deploy] démarrage du déploiement staging depuis /srv/jury-central
[deploy] branche vérifiée : develop
[deploy] working tree propre
[deploy] récupération de origin/develop
Already up to date.
[deploy] synchronisé sur origin/develop (b76c36e)
[deploy] .venv existant détecté
[deploy] .env présent (contenu non lu, non affiché)
[deploy] installation/mise à jour des dépendances (.venv existant)
[deploy] exécution de la suite de tests (pytest -q)
386 passed, 2 warnings in 17.42s
[deploy] tests au vert
[deploy] jury_central.db non modifiée (ni seed, ni reset — décision explicite séparée requise, voir docs/deployment_staging.md)
[deploy] redémarrage de jury-central.service
[deploy] jury-central.service est actif
[deploy] http://127.0.0.1:8100/health répond
[deploy] déploiement staging terminé avec succès (commit b76c36e)
```

Le script a lui-même vérifié et confirmé : branche `develop`, working tree propre, tests
verts (386 passed), DB non touchée avant redémarrage, service actif après redémarrage,
`/health` répondant. Aucune étape forcée, aucune modification de nginx/Certbot/systemd (le
script ne les touche jamais — confirmé par lecture du script avant exécution).

---

## 4. Étape 6 — `.venv/bin/seed-db`

```
$ md5sum jury_central.db
8c73a71d4bd75ea441cc721743ae759f  jury_central.db     # avant seed

$ .venv/bin/seed-db
Seed terminé : Mathématiques (MB32, MQ32, MQ34), Informatique (AMPCR)
  Créé   : 0 matière(s), 0 module(s), 0 UAA, 6 bloc(s)
  Conservé (déjà présent, non modifié) : 2 matière(s), 4 module(s), 5 UAA, 129 bloc(s)
  Retiré : 2 bloc(s) de démonstration obsolète(s)
  Reclassé (espace pédagogique COURSE/PRACTICE/EXAM mis à jour, contenu inchangé) : 16 bloc(s)
```

Correspond exactement au mécanisme documenté dans les tickets #21/#22 :
- **6 blocs créés** : les 4 exercices MC01 structurés (`editorial_exercise`, Ex1/Ex2/Ex9/Ex11
  — ticket #21) + 2 blocs Markdown réduits à nouveaux titres (les groupes d'exercices
  restants, également ticket #21).
- **2 blocs retirés** : les deux anciens blocs Markdown MC01 portant l'ancien texte des
  exercices 1/2/9/11 (`MC01_OBSOLETE_TITLES`, retrait par titre — mécanisme documenté,
  jamais un `reset-db`).
- **16 blocs reclassés** : métadonnée `space` mise à jour en place (contenu inchangé) sur
  les blocs déjà existants dont l'espace pédagogique différait de la valeur désormais
  définie dans `app/seed.py` (ticket #22) — 129 autres blocs conservés strictement
  inchangés (ni contenu ni reclassification nécessaire, déjà corrects ou déjà COURSE par
  défaut pour Mathématiques).

Aucun `reset-db`, aucune suppression de `jury_central.db`, aucune recréation de base.

---

## 5. Service et nginx

```
$ systemctl is-active jury-central.service
active
$ systemctl is-active nginx
active
```

---

## 6. Routes — local (127.0.0.1:8100) vs public (jury-central.lodylands.com)

Note technique : `curl -I` (HEAD) renvoie 405 sur ces routes — comportement Starlette/FastAPI
préexistant (HEAD non automatiquement ajouté aux routes déclarées `@app.get`), **sans lien
avec ce déploiement** ni avec #22/#23 (même comportement sur `/health`, route inchangée
depuis l'origine du projet). Vérifié en conséquence avec `GET` (`curl -s -o /dev/null -w
"%{http_code}"`), méthode strictement équivalente pour vérifier la disponibilité d'une
route.

| Route | Local | Public |
|---|---|---|
| `/health` | 200 | 200 |
| `/uaa/ampcr-mc01` | 200 | 200 |
| `/uaa/ampcr-mc01/practice` | 200 | 200 |
| `/uaa/ampcr-mc01/exam` | 200 | 200 |
| `/uaa/ampcr-mc02` | 200 | 200 |
| `/uaa/ampcr-mc02/practice` | 200 | 200 |
| `/uaa/ampcr-mc02/exam` | 200 | 200 |
| `/uaa/ampcr-mc03` | 200 | 200 |
| `/uaa/ampcr-mc03/practice` | 200 | 200 |
| `/uaa/ampcr-mc03/exam` | 200 | 200 |
| `/uaa/mb32-uaa1` (Mathématiques) | 200 | 200 |

**Local et public strictement identiques** sur les 11 routes testées.

---

## 7. Contenu par espace — MC01 (vérification exhaustive)

### COURSE (`/uaa/ampcr-mc01`)
- Théorie présente (« Vue globale d'un ordinateur » trouvé).
- Fiche mémo présente.
- Exercices absents (0 occurrence de « Exercice 1/2/9/11 — »).
- Générateur IA absent (0 occurrence).
- Examen absent (0 occurrence).
- Liens de navigation vers `/practice` et `/exam` présents.

### PRACTICE (`/uaa/ampcr-mc01/practice`)
- **Les 12 exercices présents**, chacun exactement une fois : Ex1 « Matériel ou logiciel
  (classification) », Ex2 « Unité centrale ou périphérique (classification) », Ex3-8/10/12
  (Markdown), Ex9 « Lancement d'un programme (ordering) », Ex11 « Entrée, sortie ou mixte
  (classification) ».
- Classification/ordering du ticket #21 confirmés présents et nommés correctement.
- Générateur IA présent.
- Examen absent, théorie absente.

### EXAM (`/uaa/ampcr-mc01/exam`)
- Examen présent (« Examen final » — titre de carte + titre dans le contenu, 2
  occurrences légitimes).
- **Corrigé non publié absent** (0 occurrence de « Corrigé », « 0,5 pt par rôle correct »,
  « notation qualitative »).
- Théorie et exercices absents.

---

## 8. MC02 et MC03

| | COURSE | PRACTICE | EXAM |
|---|---|---|---|
| MC02 | exercices absents, examen absent | 3 blocs d'exercices + IA présents, examen absent | examen présent (2 occurrences légitimes), corrigé absent |
| MC03 | exercices absents, examen absent | 3 blocs d'exercices + IA présents, examen absent | examen présent (2 occurrences légitimes), corrigé absent |

Identique au comportement de MC01, conforme à la répartition documentée au ticket #22.

---

## 9. Mathématiques

`/uaa/mb32-uaa1` : 200, contenu théorique intact (« Fonction constante » présent, 11
occurrences). `/uaa/mb32-uaa1/practice` et `/uaa/mb32-uaa1/exam` : 200, affichent
proprement le message d'espace vide (« Aucun exercice d'entraînement publié... » /
« Aucune évaluation publiée... ») — cohérent avec la décision explicite du ticket #22 de
laisser Mathématiques en COURSE par défaut (quiz/exercices générés non reclassés dans ce
ticket, limite assumée et déjà documentée). Aucune erreur, aucune page cassée.

---

## 10. Base de données

```
$ ls -la jury_central.db
-rw-r--r-- 1 ubuntu ubuntu 212992 ...

$ sqlite3 jury_central.db "PRAGMA integrity_check;"
ok

$ sqlite3 jury_central.db "SELECT COUNT(*) FROM subjects;"   -> 2
$ sqlite3 jury_central.db "SELECT COUNT(*) FROM modules;"    -> 4
$ sqlite3 jury_central.db "SELECT COUNT(*) FROM uaas;"       -> 5
$ sqlite3 jury_central.db "SELECT COUNT(*) FROM lesson_blocks;" -> 135

$ sqlite3 jury_central.db "SELECT u.code, COUNT(*) FROM lesson_blocks lb JOIN uaas u ON lb.uaa_id=u.id GROUP BY u.code;"
MC01|23
MC02|24
MC03|25
UAA1|25
UAA2|38

$ sqlite3 jury_central.db "PRAGMA table_info(lesson_blocks);" | grep space
7|space|VARCHAR(10)|1|'COURSE'|0

$ sqlite3 jury_central.db "SELECT space, COUNT(*) FROM lesson_blocks GROUP BY space;"
COURSE|113
EXAM|6
PRACTICE|16
```

- **`jury_central.db` toujours présent**, taille cohérente, `PRAGMA integrity_check` = `ok`.
- **Aucune perte de données** : 135 blocs au total (129 conservés + 6 créés), tous les
  comptages par UAA correspondent exactement aux attentes des rapports #21/#22 (MC01 19→23,
  MC02/MC03 inchangés en nombre de blocs mais reclassés, Mathématiques inchangée).
- **Colonne `space` présente**, valeur par défaut `'COURSE'` conforme au modèle.

### Idempotence du second `seed-db`

```
$ md5sum jury_central.db
b8777338a8ccb068cb0997b7c5c5056e  jury_central.db

$ .venv/bin/seed-db
Seed terminé : Mathématiques (MB32, MQ32, MQ34), Informatique (AMPCR)
  Créé   : 0 matière(s), 0 module(s), 0 UAA, 0 bloc(s)
  Conservé (déjà présent, non modifié) : 2 matière(s), 4 module(s), 5 UAA, 135 bloc(s)

$ md5sum jury_central.db
b8777338a8ccb068cb0997b7c5c5056e  jury_central.db
```

**0 création, 0 reclassification** (ligne « Reclassé » absente de la sortie, comportement
attendu quand rien ne change), **hash MD5 du fichier identique avant/après** — idempotence
confirmée au niveau binaire, pas seulement logique.

---

## 11. IA sans clé configurée

`OPENAI_API_KEY` **non configurée** (conformément à la consigne explicite — non modifiée
par ce déploiement) :

```
$ curl -X POST http://127.0.0.1:8100/practice/api/ai/generate \
    -H "Content-Type: application/json" -d '{"block_id": 79, "difficulty": "facile"}'
{"detail":"Génération IA non configurée sur ce serveur."}
HTTP 503
```

Réponse propre, message clair, **503** (jamais un 500). Vérifié via les journaux du service
(`journalctl -u jury-central.service`) : aucune trace d'erreur, requête traitée normalement
par le mécanisme `AINotConfiguredError` déjà en place depuis le ticket #10.

---

## 12. Ce qui n'a jamais été touché

- Aucune modification de configuration nginx (lecture seule uniquement, aucune commande
  d'écriture exécutée).
- Aucune modification Certbot.
- Aucune modification de la définition systemd du service (`systemctl cat` en lecture
  seule ; seul `systemctl restart`, déjà prévu et documenté, a été exécuté par le script).
- Aucun autre service AWS touché.
- Aucune clé OpenAI configurée.
- Aucun `reset-db`, aucune suppression ni recréation de `jury_central.db`.
- Aucune modification de code (ce déploiement ne fait que synchroniser `develop` déjà
  mergé sur GitHub — aucun commit fonctionnel dans ce rapport).

---

## Statut

Déploiement staging terminé avec succès. `jury-central.service` exécute `b76c36e`
(`develop`, à jour jusqu'au ticket #23). Contenu vérifié exhaustivement pour MC01/MC02/MC03/
Mathématiques, en local et en public. Base de données saine, idempotence confirmée. Aucune
clé OpenAI configurée — reste une action future guidée par ChatGPT. En attente de validation
ChatGPT.
